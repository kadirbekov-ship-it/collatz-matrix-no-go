# Одномерные матричные интерпретации: точный no-go

## Область утверждения

Рассматривается 11-правильная mixed-base система \(\mathcal T\) из работы
Yolcu--Aaronson--Heule, моделирующая функцию Коллатца. Каждому символу \(s\)
сопоставляется натуральная аффинная функция

\[
[s](x)=a_sx+b_s,qquad a_s\ge1,\quad b_s\ge0.
\]

Для прямого доказательства завершимости требуется строго ориентировать каждое
правило: \([\ell](x)>[r](x)\) для всех \(x\in\mathbb N\). Для аффинных функций
это требует, в частности, доминирования коэффициента при \(x\).

## Теорема

**Не существует одномерной натуральной аффинной интерпретации, строго
ориентирующей одновременно все 11 правил \(\mathcal T\).**

## Доказательство

Пять правил дают следующие необходимые неравенства для положительных целых
коэффициентов:

\[
\begin{array}{rcll}
\mathtt{<2}\to\mathtt{<ft}&:&a_2\ge a_fa_t,&(1)\\
\mathtt{t>}\to\mathtt{2>}&:&a_t\ge a_2,&(2)\\
\mathtt{f1}\to\mathtt{0t}&:&a_fa_1\ge a_0a_t,&(3)\\
\mathtt{f2}\to\mathtt{1f}&:&a_fa_2\ge a_1a_f,&(4)\\
\mathtt{t0}\to\mathtt{1t}&:&a_ta_0\ge a_1a_t.&(5)
\end{array}
\]

Из (1), (2) и положительности:

\[
a_2\ge a_fa_t\ge a_fa_2,
\]

поэтому \(a_f=1\), а затем \(a_2=a_t\). После сокращения положительных
множителей (3)--(5) превращаются в

\[
a_1\ge a_0a_2,\qquad a_2\ge a_1,\qquad a_0\ge a_1.
\]

Следовательно \(a_1\ge a_1^2\). Так как \(a_1\ge1\), получаем

\[
a_f=a_0=a_1=a_2=a_t=1.
\]

Теперь для правила \(\mathtt{f0}\to\mathtt{0f}\)

\[
[\mathtt{f0}](x)=x+b_f+b_0
=x+b_0+b_f=[\mathtt{0f}](x).
\]

Строгое неравенство невозможно. Противоречие. \(\square\)

## Двумерный булев коэффициентный класс

Проверен также следующий существенно больший класс. Каждому символу
сопоставляется функция

\[
[s](\mathbf x)=M_s\mathbf x+\mathbf v_s,
\qquad \mathbf x,\mathbf v_s\in\mathbb N^2,
\]

где все элементы \(M_s\) принадлежат \(\{0,1\}\), а
\((M_s)_{11}=1\). Векторы \(\mathbf v_s\) **не ограничиваются** сверху.
Используется стандартный порядок натуральных матричных интерпретаций:
первая координата убывает строго, вторая — не возрастает.

Для семи символов существует \(8^7=2\,097\,152\) назначений матриц. Точная
целочисленная проверка сначала оставляет 266 назначений пяти внутренних
символов, удовлетворяющих шести carry-правилам, а после добавления концевых
маркеров — 3510 полных назначений, удовлетворяющих всем матричным
неравенствам.

Для каждого из этих 3510 случаев невозможность выбора векторов доказывается
одним из четырёх сертификатов Фаркаша. Если
\(\Delta_r^{(i)}\) — линейная форма разности векторных частей правила \(r\) в
координате \(i\), то достаточно одной из комбинаций

\[
\begin{array}{l}
\Delta_{\mathtt{f0\to0f}}^{(1)},\\
\Delta_{\mathtt{t>\to2>}}^{(1)}+
  \Delta_{\mathtt{<2\to<ft}}^{(1)},\\
\Delta_{\mathtt{t2\to2t}}^{(1)},\\
\Delta_{\mathtt{t>\to2>}}^{(1)}+
  \Delta_{\mathtt{t>\to2>}}^{(2)}+
  \Delta_{\mathtt{<2\to<ft}}^{(1)}.
\end{array}
\]

В соответствующем случае все 14 коэффициентов комбинации неположительны.
При \(\mathbf v_s\ge0\) её значение поэтому не больше нуля, тогда как сумма
требуемых строгих первых координат не меньше единицы. Получается точное
противоречие \(0\ge1\). Четыре шаблона покрывают соответственно
2952, 220, 160 и 178 назначений; сумма равна 3510.

Таким образом, прямой двумерной натуральной интерпретации с булевыми
матрицами нет при **любых** натуральных векторах. Это конечная
computer-assisted теорема: пространство матриц исчерпано полностью, а каждый
лист закрывается проверяемым целочисленным двойственным сертификатом без
floating-point вычислений.

Тот же сертификатный метод полностью проведён и для элементов матриц из
\(\{0,1,2\}\). Полное пространство содержит

\[
27^7=10\,460\,353\,203
\]

назначения; точное раннее отсечение по шести внутренним правилам оставляет
4917 назначений ядра, а все 11 матричных неравенств — 299883 полных
назначения. Восемь целочисленных шаблонов Фаркаша покрывают их группами

\[
189783, 24543, 29457, 26682, 16419, 12495, 432, 72,
\]

чья сумма равна 299883. Векторы снова не ограничены. Floating-point LP
использовалось только на этапе обнаружения шаблонов; опубликованный
верификатор перебирает матрицы и проверяет противоречия исключительно целой
арифметикой.

### Снятие фиксации верхнего левого элемента

Условие \((M_s)_{11}=1\) можно убрать внутри того же коэффициентного диапазона.
Для строгой монотонности достаточно положительности этого элемента, поэтому
при ограничении всех элементов сверху двойкой остаются варианты
\((M_s)_{11}\in\{1,2\}\). Получается \(54^7=1\,338\,925\,209\,984\) сырых
назначения.

Точный перебор оставляет 19380 назначений пяти внутренних матриц и 1169880
полных назначений. Четырнадцать целочисленных шаблонов покрывают их группами

\[
\begin{split}
&(756564,49086,147210,53364,32838,24990,864,144,\\
&\qquad 64728,22854,486,4176,7920,4656),
\end{split}
\]

сумма равна 1169880. Первые восемь — прежние шаблоны, следующие шесть закрывают
новые случаи. Ещё два найденных при первоначальном CEGAR-поиске шаблона
оказались избыточными: при этом порядке проверки их покрытие равно нулю, поэтому
в опубликованный сертификат они не входят.

Следовательно прямой двумерной натуральной аффинной интерпретации нет для
**всех** матриц, каждый элемент которых принадлежит \(\{0,1,2\}\), при любых
неотрицательных векторах смещения.

### Ослабленная подсистема из восьми правил

Ни один из восьми исходных шаблонов не использует правила
\(\mathtt{t0\to1t}\), \(\mathtt{t1\to2f}\) и
\(\mathtt{<1\to<ff}\). Если удалить эти три правила до перебора, число полных
матричных назначений возрастает с 299883 до 712866, однако те же восемь
шаблонов закрывают их с покрытием

\[
(418485,83718,105453,50961,17052,36693,432,72).
\]

Таким образом, no-go доказан уже для этой строго более слабой восьмиправильной
системы. Это усиление прямой постановки; оно не означает завершимость или
незавершимость самой системы переписывания.

## Относительная постановка: строгими только два правила Коллатца

Предыдущее требование сделать строгими все 11 правил сильнее необходимого.
Девять вспомогательных правил \(\mathcal X=\mathcal A\cup\mathcal B\) сами по
себе завершаются. Поэтому полное доказательство гипотезы получилось бы уже из
относительной интерпретации, в которой только

\[
\mathtt{f>}\to\mathtt{>},\qquad
\mathtt{t>}\to\mathtt{2>}
\]

ориентированы строго, а девять правил преобразования основания — слабо. Тогда
сначала удаляются два динамических правила, после чего остаётся завершающаяся
подсистема \(\mathcal X\).

### Скалярный относительный no-go

**Не существует одномерной натуральной аффинной интерпретации, в которой оба
динамических правила строгие, а все вспомогательные правила слабые.**

Коэффициентные неравенства не зависят от того, строгим или слабым объявлено
правило. Поэтому те же пять неравенств из первой теоремы дают

\[
a_f=a_0=a_1=a_2=a_t=1.
\]

Строгое правило \(\mathtt{t>}\to\mathtt{2>}\) требует \(b_t>b_2\). Слабое
правило \(\mathtt{<2}\to\mathtt{<ft}\), после сокращения положительного
коэффициента \(a_{<}\), требует

\[
b_2\ge b_f+b_t\ge b_t.
\]

Противоречие.

Тот же вывод даёт ограниченный результат о поэтапном удалении правил. Если на
первом этапе все 11 правил ориентированы хотя бы слабо, коэффициентные
неравенства уже вынуждают единичные внутренние коэффициенты. Поэтому
\(\mathtt{f0\to0f}\) имеет одинаковые скалярные интерпретации слева и справа и
не может быть одним из правил, удаляемых строго **на первом этапе**. Это не
исключает иной первый набор правил и последующие этапы.

### Двумерный относительный no-go для коэффициентов \(\{0,1,2\}\)

Проведена полная проверка правильной относительной постановки для двумерных
натуральных матриц

\[
M_s=\begin{pmatrix}1&p_s\\q_s&d_s\end{pmatrix},
\qquad p_s,q_s,d_s\in\{0,1,2\},
\]

при произвольных смещениях из \(\mathbb N^2\). После проверки всех матричных
неравенств остаются те же 299883 назначения коэффициентов. Для каждого из них
система неравенств на смещения несовместна уже при условии, что строгими
должны быть **только два динамических правила**.

Двадцать два целочисленных шаблона Фаркаша покрывают назначения группами

\[
\begin{split}
&(145827,44529,61884,16647,3645,72,6792,1617,11661,39,762,108,\\
&\qquad 5406,21,138,156,18,54,108,189,189,21),
\end{split}
\]

сумма которых равна 299883. В правой части двойственного сертификата
учитываются только первые координаты двух динамических правил; первые
координаты вспомогательных правил, как и все вторые координаты, имеют правую
часть ноль. Поэтому это отдельная computer-assisted теорема для реального
шага rule removal, а не повтор результата «все правила строгие».

Фиксация верхнего левого элемента снимается и в относительной постановке.
Если разрешить каждый положительный вариант в том же диапазоне, то есть
\((M_s)_{11}\in\{1,2\}\), сырое пространство содержит \(54^7\) назначений,
из которых после точного матричного отсечения остаются 19380 назначений ядра и
1169880 полных назначений. Прежние 22 и 20 новых целочисленных шаблонов дают
покрытие

\[
\begin{split}
&(292338,89058,123768,33294,7290,144,222936,45522,23322,78,1524,270,\\
&10812,42,276,312,36,108,270,378,378,42,230088,27984,21360,78,4062,\\
&9324,7062,1482,480,4386,5856,270,270,378,378,708,444,960,1674,438).
\end{split}
\]

Его сумма равна 1169880. Следовательно в размерности 2 закрыт весь класс, где
**каждый** элемент каждой символьной матрицы принадлежит \(\{0,1,2\}\),
верхний левый элемент положителен, оба динамических правила строгие, девять
вспомогательных правил слабые, а векторы смещения произвольны в
\(\mathbb N^2\).

### Расширение относительной границы до коэффициента 3

Отдельный точный перебор расширяет коэффициентный куб при сохранении
\((M_s)_{11}=1\):

\[
M_s=\begin{pmatrix}1&p_s\\q_s&d_s\end{pmatrix},
\qquad p_s,q_s,d_s\in\{0,1,2,3\}.
\]

Сырое пространство содержит \(64^7=4398046511104\) назначений. После
коэффициентного отсечения остаются 47378 назначений ядра и 8546864 полных
назначения. Канонический набор из 47 целочисленных сертификатов Фаркаша имеет
покрытие

\[
\begin{split}
&(3262148,1188800,1327820,706160,344992,11248,608800,404952,49624,\\
&69172,91380,28756,15364,320,80,63156,140,2488,320,27148,100,408,\\
&816,160,200,320,576,576,100,31924,52,109040,76088,156,320,576,\\
&832,832,832,1640,52,608,312,78680,8124,144,30528),
\end{split}
\]

сумма которого равна 8546864. Самопроверяющийся C++17-перечислитель сверяет
полный вектор покрытия и возвращает ошибку при первом непокрытом назначении.
Опубликована также независимая чистая Python-реализация тех же определений;
она существенно медленнее и запускается отдельным флагом.

Фиксацию можно частично снять и здесь. Если разрешить
\((M_s)_{11}\in\{1,2\}\), на символ приходится 128 матриц, а сырое
пространство содержит \(128^7=562949953421312\) назначений. После того же
точного коэффициентного отсечения остаются 170450 назначений ядра и 33099480
полных назначений. Общий набор из 96 целочисленных сертификатов — прежние 47
и 49 дополнительных — имеет детерминированное first-match покрытие

\[
\begin{split}
&(6514396,2377600,2655640,1412320,689984,22496,1217600,8313184,99248,\\
&939440,182760,57512,30728,640,160,126312,280,4976,800,54296,200,816,\\
&1632,320,400,800,1344,1344,200,63848,104,218080,152176,312,800,1344,\\
&1664,1664,1664,3280,104,1216,624,157360,16248,288,61056,4826316,\\
&200040,158512,280,56920,243840,162656,42240,113976,40672,318368,800,\\
&800,1344,1344,165248,55896,51312,64384,281536,105512,200,172720,\\
&294168,3040,57672,320,30720,800,1344,1664,1664,1664,200,83960,\\
&12064,1296,808,880,368,680,384,384,24824,65064,14272,29848,6776,10440),
\end{split}
\]

сумма которого равна 33099480. Непокрытых назначений нет. Хеш шаблонов по
точно заданному 64-битному алгоритму равен `11289329001599812833`; тест
независимо вычисляет его из Python-таблицы и сравнивает с C++-верификатором.

Последний конечный срез снимает и это ограничение. Если **каждый** элемент
матрицы принадлежит \(\{0,1,2,3\}\), а верхний левый элемент положителен,
на символ приходится 192 матрицы и сырое пространство равно

\[
192^7=9618527719784448.
\]

Точное коэффициентное отсечение оставляет 375570 назначений ядра и 72169932
полных назначения. Вместо повторного LP-поиска для каждого листа верификатор
сначала строит двенадцать параметрических целочисленных комбинаций строк Фаркаша
и для каждого назначения заново проверяет все 14 коэффициентов. Их
first-match покрытие равно

\[
(52698288,14886420,3017712,255360,745632,26208,71532,1152,
444192,12480,6132,4824).
\]

Они закрывают все 72169932 назначения. Последнее семейство забирает ровно
бывший остаток 4824, поэтому first-match вектор всех 103 фиксированных
шаблонов теперь нулевой. Шаблоны оставлены как независимый регрессионный
корпус; их хеш по-прежнему равен `15444582427154671289`. Следовательно полный
коэффициентный куб \(\{0,1,2,3\}\) в размерности 2 закрыт при произвольных
натуральных смещениях без обращения к числовым шаблонам.

Это гибридная конечная теорема. Все двенадцать формул сами по себе являются
параметрическими конструкторами без потолка и принимаются только после точной
проверки, однако совместная теорема о покрытии пока доказана лишь для данного
куба. Из конечного покрытия не следует, что те же формулы покрывают все
произвольно большие коэффициенты; ниже отдельно выделены явные бесконечные
подсемейства.

Диагностика исходных восьми семейств показывает, что прежний остаток не был
чистым артефактом края коробки:

\[
\begin{array}{c|r|r|r}
\text{куб}&\text{выжившие}&\text{покрытие восьми}&\text{доля}\\\hline
\{0,1\}&3510&3510&100\%\\
\{0,1,2\}&1169880&1163232&99.4317365884\%\\
\{0,1,2,3\}&72169932&71702304&99.3520459462\%
\end{array}
\]

Доля слегка падает, поэтому требовалось новое структурное семейство, а не
увеличение коробки. Девятое семейство использует как веса непосредственно
\(a,b,(M_f)_{12},(M_t)_{12}\); десятое является отдельной дефицитной формой,
а одиннадцатое использует веса \(b(a+1)\), \(b((M_f)_{12}+b)\), \(b\) и
\(b(M_f)_{12}\). Вместе эти три формулы уменьшают фиксированный остаток
с 467628 до 4824; двенадцатая формула закрывает его полностью.

### Двенадцатое структурное семейство

Положим

\[
p=(M_f)_{01},\quad z=(M_0)_{01},\quad z'=(M_0)_{11},\quad
o=(M_1)_{01},\quad o'=(M_1)_{11},\quad d'=(M_2)_{11}.
\]

Двенадцатый кандидат имеет девять строк и веса

\[
\begin{array}{c|c}
\text{строка}&\text{вес}\\\hline
\texttt{dynamic-even}[1]&a\bigl(o-p(1-o')\bigr)\\
\texttt{dynamic-odd}[0]&3a\\
\texttt{dynamic-odd}[1]&b+ap+d'(az+bz')\\
\texttt{carry-f1}[0],\ \texttt{carry-f2}[0]&a\\
\texttt{carry-f1}[1]&b\\
\texttt{carry-f2}[1]&ap\\
\texttt{left-0}[0],\ \texttt{left-2}[0]&1.
\end{array}
\]

Он принимается только если все веса неотрицательны и точное повторное
сложение даёт четырнадцать неположительных коэффициентов при положительной
строгой правой части. В формуле нет потолка на элементы матриц. Например, при
\(a=37,b=113,p=1,z=o=4,z'=o'=d'=0\) комбинированный ряд имеет строгую правую
часть 111 и ненулевые коэффициенты \(-148,-37,-72\).

Как проверка масштабирования отдельно перебран срез, где шесть символов
остаются в кубе до 3, а элементы левого маркера допускают 4. Из
130631724 выживших назначений первые одиннадцать семейств оставляют 44064;
двенадцатое закрывает все 44064. Это усиливает структурную гипотезу, но не
заменяет доказательство совместной полноты двенадцати семейств для всех
натуральных двумерных матриц.

### Усиленные конечные атаки и граница вывода

Дополнительный критический прогон проверил более 5,8 миллиарда допустимых
назначений: одновременно расширялись пары символов, элементы левого маркера
до 6, верхний левый элемент до 4 и разреженные наборы больших значений вплоть
до 1000. Самый большой отдельный срез с элементами
\(\{0,1,3,11,29,83\}\) содержит 3535821372 допустимых назначения. Во всех
этих конечных срезах двенадцать конструкторов оставили ноль непокрытых.
Пробники переиспользовали `structural_family`, поэтому это независимый выбор
атак, но не независимая реализация формул.

Эти данные нельзя превращать в безусловную двумерную теорему посредством
разбиения только по \((M_f)_{10},(M_f)_{01}\) и сравнению \(a,b\). Для первого
семейства точное сложение строк даёт

\[
\begin{aligned}
C_1={}&-a v_f^{(0)}-b v_f^{(1)}\\
&+\bigl(a-a f_{00}-b f_{10}\bigr)v_t^{(0)}
 +\bigl(b-a f_{01}-b f_{11}\bigr)v_t^{(1)}\\
&+\bigl(a(t_{00}-d_{00})+b(t_{10}-d_{10})\bigr)v_{>}^{(0)}\\
&+\bigl(a(t_{01}-d_{01})+b(t_{11}-d_{11})\bigr)v_{>}^{(1)},
\end{aligned}
\]

при строгой правой части \(a>0\); здесь \(D=M_2\). Если \(f_{00}=1\), первый
коэффициент при \(v_t\) действительно равен \(-bf_{10}\). Но второй равен
\(b-af_{01}-bf_{11}\), и ещё две координаты зависят от \(T-D\).
Неравенства Фаркаша проверяются покоординатно: отрицательное значение при
\(v_t^{(0)}\) не компенсирует положительное при \(v_t^{(1)}\). Поэтому
импликация «\(f_{10}\ge1\) означает применимость семейств 1--11» из одного
этого запаса не следует.

Корректное достаточное условие для семейства 1 сильнее и уже доказано выше:
для \(\ell=(a,b)\) требуется \(\ell M_f\ge\ell\). Тогда коэффициентное
неравенство `left-2` даёт
\(\ell M_2\ge\ell M_fM_t\ge\ell M_t\), и все координаты \(C_1\)
неположительны. Чтобы доказать полноту всех двенадцати семейств, всё ещё нужно
вывести из одиннадцати матричных неравенств дизъюнкцию их **полных** условий
применимости. Конечные проверки, включая 4806144 назначения в ветке
\(f_{10}=f_{01}=0,b>a\), эту логическую импликацию не доказывают.

### Первый сертификат без потолка на коэффициенты

Конечные коробки выявляют лемму, которая уже не зависит от величины
элементов. Обозначим через

\[
\ell=e_0^{\mathsf T}M_{<}=(a,b),\qquad a\ge1,\ b\ge0
\]

первую строку матрицы левого маркера. Предположим, что выполнены все
коэффициентные неравенства одиннадцати правил и

\[
\ell M_f\ge\ell
\]

покомпонентно. Тогда в относительной постановке не существует никаких
неотрицательных векторов смещения, при которых два динамических правила
строгие, а остальные девять слабые.

Действительно, коэффициентное неравенство правила `left-2` даёт

\[
\ell M_2\ge\ell M_fM_t\ge\ell M_t.
\]

Возьмём компоненту 0 правила `dynamic-odd` с весом \(a\), компоненту 1 с
весом \(b\) и компоненту 0 правила `left-2` с весом 1. Коэффициенты при
смещениях `2` сокращаются; при `f` получается \(-\ell\), при `t` —
\(\ell-\ell M_f\), а при правом маркере — \(\ell M_t-\ell M_2\). Все они
неположительны, тогда как строгая правая часть равна \(a>0\). Получено
противоречие Фаркаша.

Это универсальная теорема для одной структурной области двумерных матриц.
Если её условие нарушено и \(\ell=(a,b)\), то автоматически

\[
b>0,\qquad (M_f)_{22}=0,\qquad a(M_f)_{12}<b.
\]

Именно эта строгая область дефицита второй координаты остаётся мишенью для
полного безграничного результата. Функция
`relative_first_row_growth_template` строит параметрический сертификат и
перепроверяет его точной целочисленной арифметикой; тест использует элементы
до 113, далеко за конечными коробками выше.

### Второй безграничный сертификат внутри области дефицита

Часть этой области закрывается другой формулой. Пусть \(e_0=(1,0)\),
\(e_1=(0,1)\), \(F=M_f\), \(T=M_t\), \(Z=M_0\), \(D=M_2\) и
\(\ell=(a,b)=e_0M_{<}\). Если, помимо одиннадцати коэффициентных неравенств,
выполнены покомпонентные условия

\[
b e_1F=0,\qquad
\ell\le \ell F+b e_1Z,\qquad
a e_0(F-I)+\ell(T-D)\le0,
\]

то сертификатом служит

\[
a\Delta_{\rm dynamic\mbox{-}even}^{(0)}+
a\Delta_{\rm dynamic\mbox{-}odd}^{(0)}+
b\Delta_{\rm dynamic\mbox{-}odd}^{(1)}+
b\Delta_{\rm carry\mbox{-}f1}^{(1)}+
\Delta_{\rm left\mbox{-}2}^{(0)}.
\]

Его коэффициенты при \(v_f\) и \(v_2\) сокращаются. При \(v_t\) остаётся
\(\ell-b e_1Z-\ell F\), при \(v_1\) — \(b e_1F\), при \(v_0\) —
\(-b e_1\), а при \(v_{>}\) — \(a e_0(F-I)+\ell(T-D)\). Поэтому все они
неположительны, а строгая правая часть равна \(2a>0\).

На точном дополнении первого семейства эта достаточная формулировка становится
полной характеризацией. Там \(f_{11}=0\), \(af_{01}<b\), и потому \(b>0\).
Условие \(b e_1F=0\) эквивалентно \(f_{10}=0\). Из `carry-f0` тогда следует
\(z_{10}=0\), а условие
\(\ell\le\ell F+b e_1Z\) эквивалентно \(z_{11}\ge1\). Последнее вместе с
`carry-f1` принуждает \(t_{10}=t_{11}=0\), после чего `carry-t2` принуждает
\(d_{10}=0\). Первая компонента неравенства при правом маркере следует из
`left-2`, поскольку \(d_{00}\ge f_{00}t_{00}\ge f_{00}+t_{00}-1\).
Остаётся ровно одна скалярная компонента. Поэтому семейство 2 применимо тогда
и только тогда, когда

\[
f_{10}=0,\qquad z_{11}\ge1,\qquad
a d_{01}+b d_{11}\ge a(f_{01}+t_{01}).
\]

Эта эквивалентность не имеет потолка на коэффициенты. Конструктор
`relative_second_deficit_template` проверяет её предпосылки и затем независимо
пересчитывает все четырнадцать коэффициентов сертификата.

Это не пустое пересечение с первой леммой. Обозначим

\[
P=\begin{pmatrix}1&0\\0&0\end{pmatrix},\qquad I=\begin{pmatrix}1&0\\0&1\end{pmatrix}.
\]

Для любых \(a\ge1,b\ge1,c,d\ge0\) положим

\[
M_f=M_t=M_1=M_2=M_{>}=P,\quad M_0=I,\quad
M_{<}=\begin{pmatrix}a&b\\c&d\end{pmatrix}.
\]

Все одиннадцать коэффициентных неравенств выполнены, но
\(\ell M_f=(a,0)\not\ge(a,b)=\ell\), так что первый сертификат не работает.
Второй даёт комбинированный ряд с единственным возможно ненулевым
коэффициентом \(-b\) и правой частью \(2a\). Тем самым внутри дефицитной
области закрыто ещё одно бесконечное четырёхпараметрическое семейство.

### Третье семейство на остатке

Тот же вывод для третьего семейства даёт комбинацию

\[
a\Delta_{\rm dynamic\mbox{-}even}^{(0)}+
b\Delta_{\rm dynamic\mbox{-}even}^{(1)}+
a\Delta_{\rm dynamic\mbox{-}odd}^{(0)}+
b\Delta_{\rm carry\mbox{-}t2}^{(1)}+
\Delta_{\rm left\mbox{-}2}^{(0)}.
\]

На дополнении первого семейства она применима тогда и только тогда, когда

\[
t_{10}=t_{11}=0,\qquad d_{11}\ge1,\qquad
a d_{01}+b\ge a(f_{01}+t_{01}).
\]

Действительно, два коэффициента при \(v_2\) равны \(bt_{10},bt_{11}\), а
коэффициент при второй компоненте \(v_t\) равен
\(b-af_{01}-bd_{11}\). Так как \(0<b-af_{01}\le b\), эти условия точно
эквивалентны нулевой нижней строке \(M_t\) и \(d_{11}\ge1\). Тогда
`carry-t2` принуждает \(d_{10}=0\); первая компонента правого маркера следует
из `left-2`, а вторая остаётся последним записанным скалярным неравенством.
Строгая правая часть равна \(2a\).

Подкласс непуст: можно взять \(M_f=M_t=M_0=M_1=M_{>}=P\), \(M_2=I\) и
произвольный \(M_<\) с положительными \(a,b\). Первое и второе семейства там
не применимы, а третье даёт точный сертификат. Это реализовано функцией
`relative_third_deficit_template` без ограничения на величину коэффициентов.

### Четвёртое семейство: точный целочисленный интервал

В четвёртом семействе свободный вес нельзя заранее отождествить с \(a\) или
\(b\). Его сертификат имеет вид

\[
a\Delta_{\rm dynamic\mbox{-}even}^{(0)}+
x\Delta_{\rm dynamic\mbox{-}even}^{(1)}+
a\Delta_{\rm dynamic\mbox{-}odd}^{(0)}+
x\Delta_{\rm dynamic\mbox{-}odd}^{(1)}+
x\Delta_{\rm carry\mbox{-}t2}^{(1)}+
\Delta_{\rm left\mbox{-}2}^{(0)},\qquad x\in\mathbb N_{>0}.
\]

На точном дополнении первого семейства четырнадцать покомпонентных условий
эквивалентны существованию целого \(x\), удовлетворяющего

\[
\begin{aligned}
1\le x&\le b, & t_{10}&=0,\\
b+x(t_{11}-2)&\le0,
&a(1-f_{00})-bf_{10}-xd_{10}&\le0,\\
x(2-d_{11})&\le af_{01},
&a(f_{00}+t_{00}-d_{00}-1)+x(f_{10}-d_{10})&\le0,\\
&&a(f_{01}+t_{01}-d_{01})+x(t_{11}-d_{11}-1)&\le0.
\end{aligned}
\]

Это не оценка и не ограниченный перебор параметра. Каждое неравенство имеет
вид \(A_j+xB_j\le0\), поэтому нижние и верхние целые границы вычисляются
точно; семейство применимо тогда и только тогда, когда итоговый интервал не
пуст. Строгая правая часть равна \(2a>0\). Функция
`relative_fourth_deficit_template` реализует эту эквивалентность и после
нахождения минимального \(x\) заново проверяет все четырнадцать коэффициентов.

Например, для

\[
F=P,\quad T=I,\quad Z=O=\begin{pmatrix}1&1\\0&0\end{pmatrix},\quad
D=\begin{pmatrix}1&0\\0&2\end{pmatrix},\quad
L=\begin{pmatrix}1&1\\0&0\end{pmatrix},\quad M_{>}=P
\]

первые три семейства не применимы, а четвёртое имеет \(x=1\) и даёт точный
сертификат. Тем самым дерево условий теперь строго разобрано для семейств
1--4 без потолка на коэффициенты.

Полные несокращённые условия применимости всех формул с фиксированными
весами, включая семейство 12, выписаны в `families-conditions.md`. Это
условия корректности отдельных сертификатов, а не доказательство того, что
их дизъюнкция следует из одиннадцати матричных неравенств.

### Пятое семейство на дополнении первой ветви

На области \(f_{11}=0\), \(af_{01}<b\) условия пятого семейства также
сокращаются без потери эквивалентности. Оно применимо тогда и только тогда,
когда

\[
\begin{gathered}
f_{00}=1,\qquad f_{01}=0,\qquad t_{11}=0,\qquad t_{10}\le f_{10},\\
2a(1-o_{00})+bf_{10}(2-o_{00})-bo_{10}\le0,\\
a(1-z_{00})+b(2f_{10}-d_{10}-z_{10})\le0,\\
t_{00}\le d_{00},\qquad t_{01}\le d_{01}.
\end{gathered}
\]

Равенства \(f_{00}=1\) и \(f_{01}=0\) следуют из коэффициентов при
\(v_1^{(0)},v_1^{(1)}\); после них две координаты при \(v_2\) дают
\(t_{11}=0\) и \(t_{10}\le f_{10}\). Остальные четыре строки являются
буквально оставшимися коэффициентами при \(v_f^{(0)},v_t^{(0)}\) и
\(v_>\). Строгая правая часть равна \(5(a+bf_{10})>0\).
`relative_fifth_deficit_template` реализует эту эквивалентность и повторно
проверяет построенный ряд по исходным правилам.

### Жёсткость диагоналей carry-правил

Шесть carry-неравенств имеют дополнительную структуру, не видимую в простом
покомпонентном списке. Для `carry-f0` и `carry-t2` сравниваются произведения
\(AB\) и \(BA\), поэтому их следы совпадают. Так как разность матриц
покомпонентно неотрицательна и имеет нулевой след, обе её диагональные
координаты равны нулю.

Оставшиеся четыре правила дают замкнутую цепочку

\[
\operatorname{tr}(FO)\ge\operatorname{tr}(ZT)
=\operatorname{tr}(TZ)\ge\operatorname{tr}(OT)
=\operatorname{tr}(TO)\ge\operatorname{tr}(WF)
=\operatorname{tr}(FW)\ge\operatorname{tr}(OF)
=\operatorname{tr}(FO).
\]

Следовательно, все знаки в цепочке являются равенствами. В каждом из шести
carry-правил обе диагональные компоненты матричного неравенства на самом деле
равны; содержательными остаются только внедиагональные компоненты. Это
безусловная алгебраическая лемма, не зависящая от потолка коэффициентов.

Есть и второй уровень жёсткости. Если неотрицательные матрицы \(A\ge B\)
имеют одинаковую диагональ, то \(\det A\le\det B\). Для четырёх центральных
carry-правил получается замкнутая цепочка

\[
\det(F)\det(O)\le\det(Z)\det(T)
\le\det(T)\det(O)\le\det(W)\det(F)
\le\det(F)\det(O).
\]

Следовательно, все определители в ней равны. Для `carry-f0` и `carry-t2`
равенство определителей следует непосредственно из
\(\det(AB)=\det(BA)\). Итак, в каждом carry-правиле сравниваемые произведения
имеют одинаковые диагональ и определитель.

Пусть их внедиагональные пары равны \((x,y)\ge(x_0,y_0)\). Равенство
определителей даёт \(xy=x_0y_0\). Если \(x_0,y_0>0\), то обязательно
\(x=x_0,y=y_0\), то есть всё матричное неравенство является равенством.
Любой строгий carry-запас возможен лишь в вырожденной ветви, где хотя бы одна
внедиагональная координата равна нулю. Поэтому оставшееся доказательство
полноты сводится к конечному разбору нулевых шаблонов, а не величин
коэффициентов.

Конечные данные указывают, что на дополнении первого семейства дополнительно
всегда \(f_{00}=1\). Однако одна лишь проверка кубов этого не доказывает.
Для полного двумерного результата требуется вывести это равенство (либо
обойти его) из диагональных равенств выше, трёх left-неравенств и
\(f_{11}=0,af_{01}<b\).

### Диагональная ветвь остатка закрыта без потолка

Первый из четырёх нулевых шаблонов матрицы \(F\) допускает короткое полное
доказательство. Положим

\[
F=\begin{pmatrix}u&0\\0&0\end{pmatrix},\qquad u\ge2,
\]

и обозначим \(Z=M_0\), \(O=M_1\), \(D=M_2\), \(G=M_>\). Пусть первая
строка \(M_<\) равна \((a,b)\), где \(a\ge1\) и \(b>0\). Тогда шесть
carry-неравенств, `left-1` и `dynamic-odd` несовместны. Следовательно, в
этой ветви никакое назначение коэффициентных матриц не может попасть в
остаток, независимо от величины их элементов.

Из `carry-f0` и `carry-f2`, с учётом равенства диагональных координат,
получаем

\[
z_{10}=0,\qquad o_{10}=0,\qquad d_{00}=o_{00}=:x.
\]

Первая координата `left-1` даёт \(x\ge u^2\). Диагональная координата
`carry-t1` даёт \(t_{00}=u\). Пусть \((c,d)^{\mathsf T}\) — первый столбец
\(G\); по соглашению \(c\ge1\). Нулевая координата `dynamic-odd` равна

\[
uc+t_{01}d\ge xc+d_{01}d.
\]

Так как \(x\ge u^2>u\), отсюда

\[
d>0,\qquad t_{01}>d_{01}. \tag{A}
\]

Остаётся два случая.

Если \(z_{11}>0\), то нижняя левая и нижняя диагональная координаты
`carry-f1` принуждают \(t_{10}=t_{11}=0\). Нижняя левая координата
`dynamic-odd`, вместе с \(c,d>0\), затем даёт \(d_{10}=d_{11}=0\).
Верхняя правая координата `carry-t2` превращается в

\[
u d_{01}\ge x t_{01},
\]

что противоречит (A) и \(x>u\).

Пусть теперь \(z_{11}=0\). Диагональные координаты `carry-f1` и
`carry-t0` дают

\[
ux=uz_{00}+z_{01}t_{10},\qquad
uz_{00}=ux+o_{01}t_{10}.
\]

Следовательно, \(z_{00}=x\) и
\(z_{01}t_{10}=o_{01}t_{10}=0\). Если бы \(t_{10}>0\), то
\(z_{01}=o_{01}=0\), а верхняя правая координата `carry-f1` потребовала бы
\(0\ge x t_{01}>0\). Поэтому \(t_{10}=0\). Диагональная координата
`carry-t2` и (A) дают \(d_{10}=0\), а нижняя левая координата
`dynamic-odd` даёт \(t_{11}\ge d_{11}\).

Сумма верхних правых координат `carry-f1` и `carry-t0` равна

\[
(u-t_{11})(o_{01}+z_{01})\ge2x t_{01}>0.
\]

Значит \(u>t_{11}\ge d_{11}\). Но верхняя правая координата `carry-t2`
требует

\[
d_{01}(u-t_{11})\ge t_{01}(x-d_{11}). \tag{B}
\]

Левая часть (B) строго меньше \(t_{01}(u-t_{11})\) по (A), тогда как
\(x\ge u^2\) и \(d_{11}\le t_{11}\) дают

\[
x-d_{11}\ge u^2-t_{11}>u-t_{11}.
\]

Правая часть (B) строго больше той же величины. Получено противоречие и во
втором случае. Тем самым диагональная ветвь закрыта. \(\square\)

Важно, что `dynamic-odd` здесь нельзя выбросить. Для любого \(u\ge2\)
назначение

\[
F=T=\operatorname{diag}(u,0),\qquad
Z=O=D=\operatorname{diag}(u^2,0)
\]

удовлетворяет всем carry-неравенствам и трём left-неравенствам при
\(f_{01}=0\), но нарушает `dynamic-odd` для любого допустимого правого
маркера. Поэтому наблюдавшееся в малом кубе отсутствие left-продолжений при
\(f_{00}>1\) само по себе было эффектом общего потолка, а не универсальной
леммой.

### Верхнетреугольная ветвь также закрыта

Пусть теперь

\[
F=\begin{pmatrix}u&p\\0&0\end{pmatrix},qquad u\ge2,\quad p>0.
\]

На дополнении первого семейства дополнительно \(ap<b\), но в доказательстве
ниже достаточно уже \(a>0\). Из диагональных координат `carry-f0` и
`carry-f2` следуют

\[
z_{10}=0,\qquad o_{10}=0,\qquad
u d_{00}+p d_{10}=u o_{00}.
\]

Положим \(x=o_{00}\). Первая координата `left-1` снова даёт \(x\ge u^2\).
Диагональная координата `carry-t1` равна

\[
t_{00}x=u d_{00},
\]

а значит

\[
t_{00}x+p d_{10}=ux,qquad t_{00}\le u. \tag{C}
\]

Для первого столбца \((c,d)^{\mathsf T}\) правого маркера верхняя координата
`dynamic-odd` имеет вид

\[
t_{00}c+t_{01}d\ge d_{00}c+d_{01}d.
\]

Но \(d_{00}=t_{00}x/u\ge u t_{00}>t_{00}\), поэтому опять

\[
d>0,\qquad t_{01}>d_{01}. \tag{D}
\]

Если \(z_{11}>0\), нижние координаты `carry-f1` дают
\(t_{10}=t_{11}=0\). Нижняя координата `dynamic-odd` затем принуждает
\(d_{10}=d_{11}=0\). Из (C) получаем \(t_{00}=u,d_{00}=x\), и верхняя
правая координата `carry-t2` снова требует
\(u d_{01}\ge x t_{01}\), что противоречит (D) и \(x>u\).

Пусть \(z_{11}=0\). Верхняя правая координата `carry-f0` даёт

\[
u z_{01}\ge pz_{00}. \tag{E}
\]

Диагональные координаты `carry-f1` и `carry-t0` равны

\[
ux=t_{00}z_{00}+z_{01}t_{10},\qquad
t_{00}z_{00}=xt_{00}+o_{01}t_{10}. \tag{F}
\]

Предположим \(t_{00}<u\). Тогда (C) даёт \(d_{10}>0\), а диагональная
координата `carry-t2`, то есть
\(t_{01}d_{10}=d_{01}t_{10}\), вместе с (D) даёт
\(d_{01}>0\) и \(t_{10}>d_{10}\). Из второй строки (F) имеем
\(z_{00}\ge x\), а из (E)

\[
z_{01}\ge \frac{px}{u}\ge pu.
\]

С другой стороны, первая строка (F) и (C) дают

\[
p d_{10}=x(u-t_{00})
=t_{00}(z_{00}-x)+z_{01}t_{10}
\ge z_{01}t_{10}>p d_{10},
\]

противоречие. Следовательно, \(t_{00}=u\), а (C) даёт \(d_{10}=0\) и
\(d_{00}=x\). Теперь (F) принуждает \(z_{00}=x\) и
\(z_{01}t_{10}=o_{01}t_{10}=0\). По (E) \(z_{01}>0\), поэтому
\(t_{10}=0\).

Нижняя координата `dynamic-odd` даёт \(t_{11}\ge d_{11}\), а диагональная
координата `carry-t1` — \(t_{11}o_{11}=0\). Если \(t_{11}=0\), то также
\(d_{11}=0\), и противоречие \(u d_{01}\ge x t_{01}\) получается сразу.
Если \(t_{11}>0\), то \(o_{11}=0\); сумма верхних правых координат
`carry-f1` и `carry-t0` даёт

\[
(u-t_{11})(o_{01}+z_{01})\ge2x t_{01}>0.
\]

Поэтому \(u>t_{11}\ge d_{11}\). Верхняя правая координата `carry-t2`
снова принимает вид (B), и те же две строгие оценки вокруг
\(t_{01}(u-t_{11})\) дают противоречие. Верхнетреугольная ветвь закрыта.
\(\square\)

### Нижнетреугольная ветвь закрыта

Пусть

\[
F=\begin{pmatrix}u&0\\r&0\end{pmatrix},\qquad u\ge2,\quad r>0,
\]

Здесь \(F^2=uF\). Из диагональных координат `carry-f0` и `carry-f2`
сразу следуют

\[
z_{01}=0,\qquad d_{01}=0. \tag{G}
\]

Рассмотрим `carry-f1`, то есть \(FO\ge ZT\). По лемме о жёсткости
сравниваемые произведения имеют одинаковые диагональ и определитель.
При этом нижняя левая координата \(FO\) равна \(r o_{00}>0\). Равенство
произведений внедиагональных координат поэтому заставляет совпасть верхние
правые координаты: если бы левая была строго больше, то при не меньшей
положительной нижней координате произведение тоже было бы строго больше.
Вместе с верхней диагональной координатой получаем

\[
u o_{00}=z_{00}t_{00},\qquad
u o_{01}=z_{00}t_{01}. \tag{H}
\]

Нижняя диагональная координата `carry-t1` равна

\[
t_{10}o_{01}+t_{11}o_{11}=0.
\]

Все множители неотрицательны, поэтому из (H) следует
\(t_{10}t_{01}=0\). Верхняя диагональная координата `carry-t0`, после
подстановки (H), принимает вид

\[
z_{00}t_{00}(u-t_{00})+u t_{01}z_{10}=0. \tag{I}
\]

Если \(t_{01}=0\), то (I) сразу даёт \(t_{00}=u\). Предположим поэтому
\(t_{01}>0\). Тогда \(t_{10}=0\), а (I) даёт \(t_{00}\ge u\).
Диагональная координата `carry-t2` вместе с (G) принуждает \(d_{10}=0\),
а её верхняя правая координата даёт \(d_{11}\ge d_{00}>0\). Нижняя левая
координата `carry-t1` теперь имеет вид

\[
t_{11}o_{10}\ge r d_{11}>0,
\]

так что \(t_{11}>0\) и \(o_{10}>0\). Из нижней диагональной координаты
того же правила следует \(o_{11}=0\). Нижняя диагональная координата
`carry-t0` затем даёт

\[
t_{11}z_{11}=o_{10}t_{01}>0,
\]

то есть \(z_{11}>0\). Нижняя левая координата `carry-f0` показывает
\(z_{11}\le z_{00}\). Но верхняя правая координата `carry-t0`, с учётом
(H), требует

\[
z_{11}\ge \frac{z_{00}}{u}(t_{00}+t_{11})>z_{00},
\]

противоречие. Значит во всех случаях

\[
t_{01}=0,\qquad t_{00}=u. \tag{J}
\]

Из (H) получаем \(o_{01}=0\) и \(o_{00}=z_{00}\). Верхняя диагональная
координата `carry-f2` теперь даёт \(d_{00}=o_{00}=z_{00}\). С другой
стороны, если \(A=au+br\), то первая координата `left-1` и нижняя левая
координата `carry-f2` дают

\[
u(a o_{00}+b o_{10})
\le A d_{00},\qquad
a o_{00}+b o_{10}\ge uA,
\]

откуда \(d_{00}\ge u^2\). Наконец, для первого столбца
\((c,d)^{\mathsf T}\) правого маркера, где \(c>0\), верхняя координата
`dynamic-odd` с учётом (G) и (J) равна

\[
uc\ge d_{00}c.
\]

Она требует \(u\ge d_{00}\ge u^2\), что невозможно при \(u\ge2\).
Нижнетреугольная ветвь закрыта без потолка. \(\square\)

Диагностическая оценка \(\rho(T)\le u\), первоначально подсказанная общим
малым кубом, при этом ложна: carry-правила допускают
\(T=\operatorname{diag}(u,N)\) со сколь угодно большим \(N\) при подходящих
вырожденных \(Z,O,D\). Истинный инвариант в доказательстве — (J), то есть
фиксируется первая строка, существенная для правого маркера, а не весь
спектральный радиус \(T\).

### Полностью внедиагональная ветвь закрыта без потолка

Последний нулевой шаблон допускает более короткое спектральное доказательство,
которое работает даже при \(u=1\). Положим

\[
F=\begin{pmatrix}u&p\\r&0\end{pmatrix},\qquad u,p,r\ge1,
\]

и обозначим через \(T,Z,O,D,G\) матрицы символов
\(t,0,1,2,>\). Пусть \(\ell=(a,b)\) — верхняя строка левого маркера;
в области дефицита \(b>ap\), поэтому обе её координаты положительны.

Сначала усилим лемму о жёсткости carry-правил. Для шести их запасов
\(\Delta_i\ge0\) и \(S=Z+O+D\) имеем

\[
\sum_i\Delta_i=(F+T)S-S(F+T)\ge0.
\]

Матрица \(F+T\) неотрицательна и неприводима, поскольку обе её
внедиагональные координаты положительны уже в \(F\). Если \(x^{\mathsf T}>0\)
и \(y>0\) — её левые и правые векторы Перрона, то

\[
x^{\mathsf T}\bigl((F+T)S-S(F+T)\bigr)y=0.
\]

Неотрицательная матрица между строго положительными векторами обязана быть
нулевой. Значит \(\sum_i\Delta_i=0\), а поскольку каждый \(\Delta_i\ge0\),
все шесть carry-неравенств являются точными равенствами. В частности,

\[
FD=OF,\qquad TO=DF,\qquad TD=DT,
\]

и потому

\[
TFD=DF^2. \tag{K}
\]

Пусть \(\lambda>1\) — корень Перрона матрицы \(F\). Её второй корень
\(\mu<0\), причём

\[
\lambda+\mu=u,\qquad \lambda\mu=-pr,
\qquad F^2=uF+prI. \tag{L}
\]

Разберём два возможных ранга \(D\); нулевой ранг исключён условием
\(d_{00}\ge1\).

**Ранг \(D\) равен единице.** Запишем
\(D=cd^{\mathsf T}\), где \(c,d\ge0\) и \(c_0d_0=d_{00}>0\).
Из \(TD=DT\) следуют

\[
Tc=\tau c,\qquad d^{\mathsf T}T=\tau d^{\mathsf T}
\]

с некоторым \(\tau>0\). Равенство (K) показывает, что

\[
TFc=\kappa c,\qquad d^{\mathsf T}F^2=\kappa d^{\mathsf T}.
\]

Матрица \(F^2\) строго положительна. Теорема Перрона даёт
\(d>0\), \(\kappa=\lambda^2\) и
\(d^{\mathsf T}F=\lambda d^{\mathsf T}\). Сравнение
\(d^{\mathsf T}TFc\) двумя способами даёт \(\tau=\lambda\).

Из \(FD=OF\) теперь

\[
O=FDF^{-1}=\lambda^{-1}Fc d^{\mathsf T}.
\]

Равенства \(FO=ZT\) и \(TZ=OT\), после умножения первого слева на
\(T\), а второго справа на \(T\), дают

\[
TF^2c=\lambda^2Fc.
\]

Подставляя (L), \(TFc=\lambda^2c\) и \(Tc=\lambda c\), получаем
\(Fc=\lambda c\). Поэтому для первого столбца \(g\) матрицы \(G\), где
\(g_0>0\), вектор \(h=Dg\) строго положителен и удовлетворяет
\(Fh=\lambda h\). Но `left-2` и `dynamic-odd` дают

\[
\ell h=\ell Dg\ge\ell FTg\ge\ell FDg
=\ell Fh=\lambda\ell h,
\]

что невозможно при \(\lambda>1\).

**Ранг \(D\) равен двум.** Тогда \(D\), а из carry-равенств также
\(O,Z,T\), обратимы. Из (K) и из пары \(FO=ZT,\ TZ=OT\) следуют

\[
TF=DF^2D^{-1},\qquad FT=ZT^2Z^{-1}. \tag{M}
\]

Следовательно, \(TF\) подобна \(F^2\), а \(FT\) подобна \(T^2\).
Равенства следов и определителей в (M) показывают, что \(T\) и \(F\)
имеют один характеристический многочлен, то есть те же корни
\(\lambda,\mu\).

Остаётся важный вопрос: совпадает ли положительное собственное направление.
Из \(FZ=ZF\) имеем

\[
Z=\alpha I+\beta F,\qquad \alpha=z_{11}\ge0,\quad
\beta=z_{01}/p=z_{10}/r\ge0.
\]

В собственном базисе \(F=\operatorname{diag}(\lambda,\mu)\) поэтому
\(Z=\operatorname{diag}(z_\lambda,z_\mu)\), где
\(z_\lambda=\alpha+\beta\lambda>0\), а \(z_\mu\ne0\) по обратимости.
Запишем в этом базисе

\[
T=\begin{pmatrix}A&B\\C&E\end{pmatrix}.
\]

Равенство \(FTZ=ZT^2\) и тождество \(T^2=uT+prI\) дают по диагонали
\(A=\lambda,\ E=\mu\). Равенство определителей затем даёт \(BC=0\).
Если бы \(C\ne0\), нижняя левая координата потребовала бы

\[
\frac{z_\lambda}{z_\mu}=\frac{u}{\mu}.
\]

После подстановки \(z_\lambda,z_\mu\) это превращается в

\[
\lambda\alpha+\mu^2\beta=0,
\]

невозможное для \(\alpha,\beta\ge0\), не равных одновременно нулю.
Значит \(C=0\), и правый вектор Перрона \(v>0\) матрицы \(F\) является
также правым вектором Перрона \(T\): \(Fv=Tv=\lambda v\).

Поскольку \(TD=DT\), существует \(\delta>0\) такое, что
\(Dv=\delta v\) и \(x_T^{\mathsf T}D=\delta x_T^{\mathsf T}\) для
левого вектора Перрона \(x_T^{\mathsf T}>0\) матрицы \(T\).
Из `dynamic-odd` следует

\[
\lambda x_T^{\mathsf T}G\ge\delta x_T^{\mathsf T}G,
\qquad\text{то есть}\qquad \delta\le\lambda.
\]

С другой стороны, `left-2`, умноженное справа на \(v\), даёт

\[
\delta\ell v\ge\ell FTv=\lambda^2\ell v,
\qquad\text{то есть}\qquad \delta\ge\lambda^2.
\]

Это снова противоречит \(\lambda>1\). Таким образом, полностью
внедиагональная ветвь невозможна при любом \(u\ge1\). \(\square\)

В частности, четыре нулевых шаблона закрывают всю предполагаемую область
\(f_{00}\ge2\), поэтому в дополнении первого семейства строго доказано
\(f_{00}=1\). Более сильный вывод последней ветви дополнительно исключает
\(f_{01}f_{10}>0\) и при \(f_{00}=1\).

Зафиксируем явно, что этот переход даёт исчерпывающую классификацию, а не
перечень найденных примеров. На дополнении первого семейства уже выведено

\[
f_{11}=0,\qquad af_{01}<b,
\]

а стандартное условие строгой монотонности натуральной матричной
интерпретации даёт \(f_{00}\ge1\). Предыдущие четыре ветви поэтому оставляют
только

\[
f_{00}=1,\qquad f_{11}=0,\qquad f_{01}f_{10}=0.
\]

Положив \(p=f_{01}\) и \(r=f_{10}\), получаем

\[
F=\begin{pmatrix}1&p\\r&0\end{pmatrix},\qquad
F^2=\begin{pmatrix}1+pr&p\\r&pr\end{pmatrix}=F.
\]

Поскольку \(p,r\in\mathbb N\) и \(pr=0\), имеется ровно три взаимно
исключающих случая: \(p=r=0\), \(p\ge1,r=0\) и \(p=0,r\ge1\). Обратное
также непосредственно: каждая из этих матриц идемпотентна. Следовательно,
никакой четвёртой формы в остатке нет. Для полного двумерного no-go остаётся
доказать покрытие именно этих трёх идемпотентных форм

\[
F^2=F,\qquad
F\in\left\{
\begin{pmatrix}1&0\\0&0\end{pmatrix},
\begin{pmatrix}1&p\\0&0\end{pmatrix},
\begin{pmatrix}1&0\\r&0\end{pmatrix}
\right\},
\]

условиями семейств 2--12. Конечное покрытие остаётся подсказкой ветвления,
но не заменяет эту последнюю дизъюнкцию.

### Шестое семейство

На том же дополнении одиннадцать общих условий шестого семейства сокращаются
до

\[
M_f=\begin{pmatrix}1&0\\0&0\end{pmatrix},\qquad
t_{00}=1,\quad t_{01}=t_{10}=0,\quad t_{11}\le1,qquad
a(o_{01}+z_{01})\ge2b.
\]

Это эквивалентность: коэффициенты при \(v_2\) сначала принуждают
\(f_{00}=1,f_{01}=f_{10}=0\); координаты при \(v_0,v_1\) дают записанную
форму \(M_t\); после подстановки единственным неавтоматическим условием
остаётся \(a(o_{01}+z_{01})\ge2b\). Правая часть равна \(6a>0\).
Конструктор `relative_sixth_deficit_template` и отдельный пример подтверждают
непустоту этой безграничной ветви.

### Седьмое семейство

Пусть \(D=b-a\). На дополнении первой ветви седьмое семейство невозможно при
\(D\le0\): тогда из \(af_{01}<b\le a\) следует \(f_{01}=0\), но коэффициент
при \(v_t^{(1)}\) остаётся равным \(b>0\). При \(D>0\) точные условия равны

\[
t_{00}=1,\qquad t_{01}=0,\qquad
Dd_{01}\ge b-af_{01},\qquad
(a,b)M_t\le(a,b)M_2.
\]

Остальные коэффициенты автоматически неположительны из натуральности и
\(f_{00},d_{00}\ge1\). Правая часть равна \(2a>0\). Функция
`relative_seventh_deficit_template` реализует эту эквивалентность; отдельный
пример с \(a=1,b=2,f_{01}=1\) не закрывается семействами 1--6 и закрывается
седьмым.

### Восьмое семейство

Оно невозможно при \(b\le a\). При \(D=b-a>0\) и
\(E=\lceil D/4\rceil\) его точные условия на дополнении первой ветви равны

\[
\begin{gathered}
f_{00}=t_{00}=1,\qquad f_{10}=0,\qquad f_{01}\le1,\qquad t_{01}=0,\\
E(1-o_{10})\le a,qquad E(o_{01}+z_{01})\ge b-af_{01},\\
(a,b)M_t\le(a,b)M_2.
\end{gathered}
\]

При \(E>0\) остальные условия либо дают записанные нули и единицы, либо
автоматически следуют из натуральности. Конструктор
`relative_eighth_deficit_template` проверяет сокращённую формулу независимо
от перекрытия с более ранними семействами.

### Девятое и десятое семейства

На дополнении первой ветви девятое семейство эквивалентно

\[
\begin{gathered}
f_{00}=t_{00}=1,qquad f_{10}t_{01}=0,qquad f_{01}f_{10}=0,\\
f_{01}+t_{01}\le b+f_{01}o_{11}+t_{01}z_{11},\\
o_{01}+z_{01}\ge b-af_{01},qquad(a,b)M_t\le(a,b)M_2.
\end{gathered}
\]

Для десятого положим \(S=\max(1,b-1)\). Его точные сокращённые условия:

\[
\begin{gathered}
f_{00}=t_{00}=1,\quad f_{10}=0,\quad t_{01}\le2,\quad
f_{01}\le S,\quad a\le S,\\
S(1-o_{11})-b-z_{11}+2\le0,\\
S-a-o_{00}-z_{00}-z_{10}+1\le0,\\
o_{01}+z_{01}+z_{11}\ge b-af_{01},qquad
(S,b)M_t\le(S,b)M_2.
\end{gathered}
\]

Конструкторы `relative_ninth_deficit_template` и
`relative_tenth_deficit_template` проверяют эти эквивалентности точной
арифметикой. Ветки перекрываются с ранними семействами; это допустимо, потому
что для полноты нужна покрывающая дизъюнкция, а не разбиение.

### Одиннадцатое семейство

На дополнении первого семейства оно применимо тогда и только тогда, когда

\[
\begin{gathered}
a\le b,\qquad f_{00}=1,\qquad f_{10}=0,\\
b+f_{01}o_{11}+o_{01}\ge f_{01}+1,\\
af_{01}+z_{01}+z_{11}+1\ge b+f_{01},\\
(a+1,b+f_{01})M_t\le(a+1,b+f_{01})M_2.
\end{gathered}
\]

Все прочие координаты его общего ряда становятся автоматическими после
\(f_{11}=0\) и записанной формы первого столбца \(M_f\). Строгая правая
часть \(b(a+1)>0\). Это реализовано в
`relative_eleventh_deficit_template`.

### Двенадцатое семейство в сокращённой форме

На дополнении первой ветви положим

\[
p=f_{01},\quad K=az_{01}+bz_{11},\quad
q=a\bigl(o_{01}-p(1-o_{11})\bigr),\quad H=ap+d_{11}K+b.
\]

Тогда семейство 12 применимо в точности при

\[
\begin{gathered}
f_{00}=1,\qquad f_{10}=0,\qquad q\ge0,\qquad(d_{11}-1)K\le0,\\
3a(t_{00}-d_{00})+H(t_{10}-d_{10})\le0,\\
3a(t_{01}-d_{01})+H(t_{11}-d_{11})-q\le0.
\end{gathered}
\]

Первое неравенство обеспечивает неотрицательность единственного потенциально
знакового веса. Равенства для первого столбца \(M_f\) следуют из координаты
при \(v_1^{(0)}\); после их подстановки все внутренние коэффициенты, кроме
\((d_{11}-1)K\), становятся автоматическими. Последние две строки — ровно
две координаты правого маркера. Строгая правая часть равна \(3a>0\).
`relative_twelfth_gap_template` проверяет эту сокращённую характеризацию на
обычном и крупнокоэффициентном свидетелях прежнего зазора.

### Покрытие трёх идемпотентных форм

Оставшаяся дизъюнкция также выводится без потолка. Сначала рассмотрим

\[
F=\begin{pmatrix}1&0\\r&0\end{pmatrix},\qquad r\ge1.
\]

Доказательство нижнетреугольной леммы до формулы (J) не использовало
\(u\ge2\). Поэтому здесь

\[
t_{00}=1,\quad t_{01}=0,\quad
z_{01}=o_{01}=d_{01}=0,\quad z_{00}=o_{00}=d_{00}.
\]

Верхняя координата dynamic-odd на первом столбце правого маркера даёт
\(1\ge d_{00}\), откуда три последние верхние левые координаты равны
единице. Нижняя левая координата carry-f2 и первая координата left-1 дают

\[
r\ge o_{10}+r o_{11},\qquad o_{10}\ge r,
\]

то есть \(o_{10}=r,\ o_{11}=0\). Далее carry-f0, left-0 и нижняя левая
координата carry-t0 дают

\[
r\ge z_{10}+r z_{11},\qquad
z_{10}\ge t_{10},\quad z_{11}\ge t_{11},\qquad
t_{10}+t_{11}z_{10}\ge r.
\]

Если \(z_{11}=1\), первое неравенство даёт \(z_{10}=0\), что противоречит
последнему. Поэтому \(z_{11}=t_{11}=0\), а затем
\(t_{10}=z_{10}=r\). Из left-2 следует \(d_{10}\ge r\).
Это в точности все условия семейства 5:

\[
t_{11}=0,\quad t_{10}\le r,\quad o_{10}\ge r,\quad
d_{10}+z_{10}\ge2r,\quad t_{00}\le d_{00},\quad t_{01}\le d_{01}.
\]

Теперь положим \(F=P=\operatorname{diag}(1,0)\). Из carry-f0 и carry-f2
следуют \(z_{10}=o_{10}=0\) и \(d_{00}=o_{00}\).

Если \(z_{11}\ge1\), вторая координата left-2 является ровно последним
условием семейства 2, поэтому оно применимо.

Пусть \(z_{11}=0\). Если бы \(t_{10}>0\), верхнее диагональное равенство
carry-f1 и dynamic-odd дали бы

\[
t_{00}\ge d_{00}=o_{00}
=z_{00}t_{00}+z_{01}t_{10}\ge t_{00}.
\]

Отсюда \(z_{00}=1,\ z_{01}=0\), но первая координата left-0 потребовала бы
\(a\ge at_{00}+bt_{10}>a\). Следовательно, \(t_{10}=0\), а dynamic-odd
даёт \(d_{10}=0\). Диагональные равенства, left-0 и left-2 теперь
принуждают

\[
z_{00}=o_{00}=d_{00}=t_{00}=1.
\]

Обозначим

\[
s=t_{01},\quad y=t_{11},\quad z=z_{01},\quad
o=o_{01},\quad q=o_{11},\quad d=d_{01},\quad e=d_{11}.
\]

Из carry-f1, carry-t0 и left-0 получаем

\[
o\ge s+zy,\qquad z\ge s+oy,\qquad az\ge as+by,\qquad qy=0.
\]

Если \(y>0\), эти неравенства вынуждают
\(y=1,\ s=0,\ o=z\) и \(az\ge b\); это ровно семейство 6.
Если \(y=0\) и \(s\le d\), применимо семейство 5.
Наконец, при \(y=0,\ s>d\) left-2 даёт

\[
ad+be\ge as,
\]

поэтому \(e\ge1\). При \(e=1\) это условие семейства 3. При \(e\ge2\)
семейство 4 применимо с

\[
x=\max\left\{\left\lceil\frac b2\right\rceil,
\left\lceil\frac{a(s-d)}{e+1}\right\rceil\right\}\le b.
\]

Неравенство \(x\le b\) следует из \(a(s-d)\le be\); все остальные
ограничения четвёртого семейства после подстановки автоматические.
Проекционная форма полностью покрыта семействами 2--6.

Остаётся верхняя форма

\[
F=\begin{pmatrix}1&p\\0&0\end{pmatrix},\qquad p\ge1,\qquad b>ap.
\]

Из carry-f0 и carry-f2 имеем \(z_{10}=o_{10}=0\). Покажем сначала, что
\(t_{10}=0\). Если \(t_{10}>0\), то \(F+T\) неприводима, а лемма Перрона
выше превращает все carry-неравенства в равенства. Из нижней левой
координаты \(FO=ZT\) следует \(z_{11}=0\), после чего \(FZ=ZF\) даёт
\(Z=cF\), \(c=z_{00}\ge1\). Запишем

\[
T=\begin{pmatrix}x&y\\s&w\end{pmatrix},\qquad s>0.
\]

Равенства \(FO=ZT\) и \(TZ=OT\) последовательно дают

\[
o_{11}=c,\quad w=ps,\quad
o_{00}=c(x+ps),\quad
o_{01}=c(y+p^2s-p).
\]

Верхняя левая координата \(TZ=OT\) тогда равна

\[
0=(x-1)(x+ps)+ys+p^2s^2,
\]

что невозможно. Значит \(t_{10}=0\). Dynamic-odd даёт \(d_{10}=0\), а
диагональные carry-равенства вместе с left-0 и left-2 дают

\[
z_{00}=o_{00}=d_{00}=t_{00}=1.
\]

Положим

\[
s=t_{01},\quad w=t_{11},\quad z=z_{01},\quad k=z_{11},\quad
o=o_{01},\quad q=o_{11},\quad d=d_{01},\quad e=d_{11}.
\]

Все оставшиеся коэффициентные обязательства, нужные ниже, имеют скалярный
вид

\[
\begin{gathered}
z+pk\ge p,\qquad o+pq\ge s+zw,\qquad d+pe\ge p,\\
z+sk\ge s+ow,\qquad o+sq\ge p,\qquad
d+se\ge s+dw,\\
az+bk\ge as+bw,\qquad ao+bq\ge ap,\qquad
ad+be\ge a(s+pw),\\
kw=qw=0.
\end{gathered} \tag{N}
\]

Если \(w>0\), то \(k=q=0\). Первые carry-условия в (N) дают

\[
o\ge s+zw,\qquad z\ge s+ow.
\]

Так как \(z,o\ge p>0\), обязательно \(w=1,\ s=0,\ z=o\).
При \(e\le1\) применимо семейство 12. Для \(e=1\) его последнее условие
очевидно; для \(e=0\) оно после сокращения равно

\[
b+2ap-ao-3ad\le0,
\]

что следует из \(az\ge b,\ o=z\) и \(d\ge p\). При \(e\ge2\) применимо
семейство 4 с \(x=b\); его последнее условие есть
\(a(p-d)-be\le0\), то есть одно из неравенств (N).

Пусть \(w=0\). При \(e\le1\) применимо семейство 12. Действительно, из
\(o+pq\ge s\) и \(o+sq\ge p\) в обоих случаях \(s\ge p\) и \(s<p\)
следует \(o+pq\ge p\). Если \(e=0\), то carry-t2 даёт \(d\ge s\), и
последнее условие семейства 12 равно

\[
3a(s-d)-b-a(o+pq)\le0.
\]

Если \(e=1\), то то же условие следует из

\[
az+bk\ge as,\qquad a(o+pq)\ge as,\qquad ad+b\ge as.
\]

Наконец, при \(e\ge2\) семейство 4 снова работает с \(x=b\), поскольку

\[
a(p+s-d)-b(e+1)\le ap-b<0.
\]

Тем самым верхняя форма покрыта семействами 4 и 12. Вместе с первым
семейством, четырьмя исключениями для \(f_{00}\ge2\) и полностью
внедиагональной леммой это доказывает покрывающую дизъюнкцию для всех
натуральных двумерных коэффициентов без потолка.

### Полная двумерная теорема

Для смешанной системы \(T\) не существует относительной натуральной
аффинной матричной интерпретации размерности 2, в которой оба динамических
правила строги, а остальные девять правил слабы. Ограничений на элементы
матриц и векторы смещения нет.

Доказательство является конструктивным: если выполнены все одиннадцать
коэффициентных неравенств, то либо применяется семейство 1, либо описанный
выше структурный разбор приводит к одному из семейств 2--12. Каждое
семейство является точным неотрицательным сертификатом Фаркаша с
положительной строгой правой частью. Если коэффициентное неравенство
нарушено, матричная интерпретация отсутствует уже до выбора смещений.
\(\square\)

Функция `relative_structural_farkas_template` реализует все двенадцать формул
без потолка на входные коэффициенты и принимает результат только после точной
проверки. В четвёртой формуле свободный вес
\(x\ge1\) не перебирается до искусственной границы: система четырнадцати
линейных ограничений \(A_j+xB_j\le0\) решается как точный целочисленный
интервал. После проверки всех коэффициентных неравенств отсутствие подходящей
формулы считается нарушением доказанной структурной теоремы, а не
неопределённым исходом.

Результат исключает относительные натуральные аффинные интерпретации в
размерностях 1 и 2 без ограничения на коэффициенты. Он не исключает
размерность 3 и выше, арктические или тропические интерпретации, dependency
pairs или несколько последовательных стадий удаления правил.

## Что доказано и чего не доказано

Результат закрывает весь бесконечный класс скалярных натуральных аффинных
интерпретаций и весь класс двумерных натуральных относительных интерпретаций
с двумя строгими динамическими правилами. Обе части являются алгебраическими
теоремами, а не выводами из ограниченного поиска.

Совместно эти теоремы **не** исключают:

- матрицы размерности \(d\ge3\);
- арктические или тропические интерпретации;
- dependency pairs;
- относительные и поэтапные доказательства за пределами закрытого выше класса;
- иные системы переписывания, эквивалентные Коллатцу.

Двумерная теорема закрывает все натуральные коэффициенты. Поэтому результат
не решает гипотезу Коллатца: он удаляет один точно заданный язык потенциальных
доказательств, тогда как другие размерности и методы остаются открытыми.

## Воспроизведение

```bash
python3 -m sag_collatz.matrix_attack
python3 -m sag_collatz.matrix_attack --matrix-entry-max 2
python3 -m sag_collatz.matrix_attack --matrix-entry-max 2 --upper-left-max 2
python3 -m sag_collatz.matrix_attack --matrix-entry-max 2 --relative
python3 -m sag_collatz.matrix_attack --matrix-entry-max 2 --upper-left-max 2 --relative
python3 -m sag_collatz.matrix_attack --relative-cubic  # медленный Python-пересчёт
python3 -m sag_collatz.matrix_attack --matrix-entry-max 2 --upper-left-max 2 --relative-cubic
python3 -m sag_collatz.matrix_attack --matrix-entry-max 2 --weakened-eight-rules
clang++ -O3 -std=c++17 tools/verify_relative_cubic.cpp -o /tmp/verify-relative-cubic
/tmp/verify-relative-cubic
/tmp/verify-relative-cubic --upper-left-two
/tmp/verify-relative-cubic --upper-left-three
/tmp/verify-relative-cubic --upper-left-three --left-entry-four
python3 -m unittest discover -s tests -v
```

Источник системы и определения интерпретаций:
https://arxiv.org/abs/2105.14697
