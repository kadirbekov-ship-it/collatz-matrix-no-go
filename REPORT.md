# Сократический граф доказательств: конечный эксперимент Коллатца

## Итог

Для ускоренного отображения Коллатца

\[
T(n)=\frac{3n+1}{2^{v_2(3n+1)}}
\]

исчерпывающе рассмотрены все нечётные классы по модулю
\(2^{20}=1\,048\,576\). Для **496 959 из 524 288 классов** построен и
независимо проверен сертификат того, что каждый представитель класса имеет
итерацию, строго меньшую исходного числа. Это

\[
\frac{496959}{524288}=94.78740692138672\%.
\]

Остальные 27 329 классов помечены как **неразрешённые**, а не ложные. Результат
является конечной теоремой о плотности классов вычетов и не является
доказательством гипотезы Коллатца.

Важно: спуск некоторой итерации ниже исходного числа не означает сходимость
траектории к 1. Поэтому 94,79% сертифицированных классов нельзя называть 94,79%
чисел, для которых доказана гипотеза Коллатца.

## Точная формулировка

Пусть \(k=20\), \(r\) — нечётное, \(1\le r<2^k\), и

\[
C_{r,k}=\{r+2^kq:q\in\mathbb Z_{\ge0}\}.
\]

Для каждого из 496 959 сертифицированных значений \(r\) существует
\(1\le j\le12\), такое что

\[
\forall n\in C_{r,k}\quad T^j(n)<n.
\]

Таким образом, сертифицированные классы имеют относительную натуральную
плотность \(496959/524288\) среди нечётных положительных чисел.

## Почему сертификат распространяется на бесконечный класс

Запишем траекторию представителя \(r\):

\[
x_0=r,\qquad a_i=v_2(3x_{i-1}+1),\qquad
x_i=\frac{3x_{i-1}+1}{2^{a_i}},\qquad
A_i=\sum_{t=1}^{i}a_t.
\]

### Точный сертификат

Если \(A_i<k\) до шага \(j\), то индукцией по \(i\) для любого \(q\ge0\)
получаем точное равенство

\[
T^j(r+2^kq)=x_j+3^j2^{k-A_j}q.
\]

Причина: добавка, зависящая от \(q\), содержит больше степеней двойки, чем
основная часть, поэтому значения \(a_i\) до исчерпания точности одинаковы для
всего класса. Если одновременно

\[
x_j<r\quad\text{и}\quad 3^j<2^{A_j},
\]

то и постоянная часть, и коэффициент при \(q\) строго меньше соответствующих
частей \(r+2^kq\). Следовательно, \(T^j(n)<n\) для всего класса.

### Усечённый сертификат на границе точности

Пусть \(A_{j-1}<k\), но

\[
A_{j-1}+v_2(3x_{j-1}+1)\ge k.
\]

Обозначим \(d=k-A_{j-1}\). Для всех \(q\ge0\) гарантировано деление как
минимум на \(2^d\), поэтому

\[
T^j(r+2^kq)
\le \frac{3x_{j-1}+1}{2^d}+3^jq.
\]

Если правая постоянная часть меньше \(r\), а \(3^j<2^k\), верхняя граница
меньше \(r+2^kq\). Возможное дополнительное деление на двойку только усиливает
неравенство. Таких пограничных сертификатов найдено 4 403.

## Результаты проверки

| Показатель | Значение |
|---|---:|
| Всего нечётных классов | 524 288 |
| Точные сертификаты | 492 556 |
| Усечённые сертификаты | 4 403 |
| Всего сертифицировано | 496 959 |
| Не разрешено этим протоколом | 27 329 |
| Максимальный использованный шаг | 12 |

Распределение по первому сертифицирующему шагу:

| \(j\) | Число классов |
|---:|---:|
| 1 | 262 143 |
| 2 | 65 536 |
| 3 | 65 536 |
| 4 | 24 576 |
| 5 | 28 672 |
| 6 | 12 288 |
| 7 | 7 680 |
| 8 | 10 880 |
| 9 | 5 536 |
| 10 | 7 616 |
| 11 | 3 844 |
| 12 | 2 652 |

SHA-256 двоичного сертификата:

```text
4d9e1a334d72a472639ad49d806246f022f39d4567b60d915b37d6a311cd507b
```

Независимый верификатор заново проверил каждый ненулевой байт, восстановил
канонический результат также для нулевых байтов и получил те же счётчики.
Дополнительно 10 000 сертификатов проверены прямой подстановкой пяти значений
\(q\), включая случайные 1024-битные числа: всего 50 000 конкретных проверок.
Последнее является только стресс-тестом реализации; доказательную силу дают
точные неравенства выше.

## Состязательные опровержения

Критик проверил слишком сильное семейство утверждений:

> Каждое нечётное \(n>1\) имеет итерацию меньше \(n\) не позднее \(L\)
> ускоренных шагов.

Минимальные найденные контрпримеры:

| \(L\) | Контрпример |
|---:|---:|
| 1 | 3 |
| 2 | 7 |
| 3 | 7 |
| 4 | 27 |
| 10 | 27 |
| 20 | 27 |
| 36 | 27 |

Число 27 впервые опускается ниже старта на ускоренном шаге 37, достигая 23.
Это демонстрирует, почему отсутствие контрпримера в коротком тесте нельзя
считать доказательством.

## Архитектура графа

Корень — множество положительных нечётных чисел. На глубине \(k\) его узлы —
классы \(C_{r,k}\). Генератор предлагает для узла точный или пограничный
сертификат. Критик независимо восстанавливает траекторию и проверяет:

1. корректность всех 2-адических порядков;
2. отсутствие преждевременного исчерпания точности;
3. спад постоянной части;
4. коэффициент строго меньше единицы;
5. каноничность первого принятого шага.

Непрошедший узел остаётся открытым. Вероятностный вес и Монте-Карло допустимы
для выбора следующего открытого узла, но не влияют на доказательный статус.

## Воспроизведение и аудит

Команды приведены в `README.md`. Основные артефакты находятся в `artifacts/`:

- `collatz-k20-l63.bin` — один байт на нечётный класс;
- `collatz-k20-l63.json` — метаданные и контрольная сумма;
- `independent-audit.json` — результат точного критика;
- `bigint-stress.json` — недоказательный стресс-тест больших подстановок;
- `adversarial-counterexamples.json` — опровержения усиленных гипотез;
- `coverage-by-depth.csv` — покрытие на нескольких глубинах.

Готовый запрос для внешнего аудита Claude находится в
`CLAUDE_AUDIT_PROMPT.md`.

Результат выполненного независимого аудита сохранён в `EXTERNAL_AUDIT.md`.

## Ограничения

- 27 329 открытых классов не покрыты этим сертификатом.
- Проверка любых конечных классов сама по себе не доказывает Коллатца.
- Генератор и критик написаны в одном проекте и могут разделять ошибку в
  математической постановке; поэтому подготовлен внешний аудит.
- SHA-256 защищает воспроизводимость файла, но не доказывает математику.
